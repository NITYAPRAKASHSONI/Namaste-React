const heading = React.createElement(
    "h1",
    {},
    "Namaste React from React CDN"
);
console.log(heading)
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);


